#RELEASE Location of external products
# Run "gnumake clean uninstall install" in the application
# top directory each time this file is changed.

# Define locations of top-level directories

#---------------------
# EPICS_BASE usually appears last so other apps can override stuff:
#
include $(TOP)/../../configure/RELEASE-base
include $(TOP)/../../configure/RELEASE-sncseq
include $(TOP)/../../configure/RELEASE-generalTime
SSUBRECORD=$(TOP)/../..
STD=$(TOP)/../..

# Libraries used by EVR IOCs
EVR_IOC_LIBS=evrSupport drvSupport sSubRecord generalTime

